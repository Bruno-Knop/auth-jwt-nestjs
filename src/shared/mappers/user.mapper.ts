import { plainToClass } from 'class-transformer';

export class UserMapper {
  // static toUser(userOrmEntity: UserOrmEntity): User {
  //   return plainToClass(User, userOrmEntity);
  // }
  // static toUsers(userOrmEntities: UserOrmEntity[]): User[] {
  //   return userOrmEntities.map((userOrmEntity) =>
  //     plainToClass(User, userOrmEntity),
  //   );
  // }
}
